# Copy to `.env.local` for local dev (Next.js loads `.env.local` automatically; do not commit secrets).
# Server-only variable (not prefixed with NEXT_PUBLIC_). Read on /env-demo.

LESSON_DEMO_MESSAGE="Hello from the environment"
